import 'package:flutter/material.dart';

class ScheduleAvailabilityScreen extends StatelessWidget {
  const ScheduleAvailabilityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tạo Khung giờ rảnh')),
      body: const Center(
        child: Text('Module 9: Bác sĩ set ca làm việc trong tuần'),
      ),
    );
  }
}
